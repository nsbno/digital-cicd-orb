description: >
  Sample description
# What will this job do?

executor: default

parameters:
steps:
  - build_status_command:
      # repo_name: << pipeline.project_name >>
      job_name: ${CIRCLE_JOB}
